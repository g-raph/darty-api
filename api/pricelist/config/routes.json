{
  "routes": [
    {
      "method": "GET",
      "path": "/pricelist",
      "handler": "pricelist.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pricelist",
      "handler": "pricelist.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pricelist",
      "handler": "pricelist.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
